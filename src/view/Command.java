package view;

/**
 * Used to display strings on components and also to set action commands
 */
public enum Command {
    HOME,
    PRESENCE,
    ADD,
    DELETE,
    CHARTS,
    GROUP_NUMBER_CHANGED,
    WEEK_NUMBER_CHANGED
}
